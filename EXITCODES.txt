0 (anywhere) success

program:
0 success/catched exception (check log.txt)
1 exception when running cli tool, and earlier than cli tool would catch the error itself
2 could not log the crash
3 could not write to errorFile


mod tool:

create:
1 directory with same name already exists


validate:
1 issue with mod's name or version